import onChange from "on-change";
import axios from 'axios';

const state = {
    elements: {
      input: document.getElementById('url-input')
},
    feeds: [],
    posts: [],
    url: '',
}

const validateForm = () => {

}
export default (() => {
    console.log('hello');
})